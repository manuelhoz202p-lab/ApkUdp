- name: Build with Gradle (save log)
  run: |
    ./gradlew assembleDebug --stacktrace --info > buildlog.txt 2>&1

- name: Upload Build Log
  if: always()
  uses: actions/upload-artifact@v4
  with:
    name: gradle-build-log
    path: buildlog.txt
